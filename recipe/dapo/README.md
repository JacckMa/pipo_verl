# DAPO Recipe

This directory keeps the DAPO trainer components used by the PIPO scripts.

Public experiment entrypoints are under:

```bash
scripts/math/
scripts/code/
scripts/tooluse/
scripts/sciknoweval/
```

For DAPO runs, use the corresponding `run_*_dapo*.sh` scripts in those folders.
